# SDKWork Memory Local Plugins

No repository-local plugins are defined yet.

Add local plugins only when this repository needs checked-in plugin metadata. Do not place generated SDK control-plane files here.
