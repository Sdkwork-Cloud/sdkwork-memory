# Config templates

Safe checked-in config templates for dev/test/staging/prod per `CONFIG_SPEC.md` and `ENVIRONMENT_SPEC.md`.

Local overrides such as `.env.local` and `config/*.local.toml` must remain gitignored.
