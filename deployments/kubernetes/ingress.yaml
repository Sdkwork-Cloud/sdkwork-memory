apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: sdkwork-api-memory-standalone-gateway
  labels:
    app.kubernetes.io/name: sdkwork-memory
    app.kubernetes.io/component: standalone-gateway
    app.kubernetes.io/part-of: sdkwork-memory
  annotations:
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-protocols: "TLSv1.2 TLSv1.3"
    nginx.ingress.kubernetes.io/proxy-body-size: "1m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    cert-manager.io/cluster-issuer: "sdkwork-prod-issuer"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.sdkwork.com
      secretName: sdkwork-memory-tls
  rules:
    - host: api.sdkwork.com
      http:
        paths:
          - path: /mem/v3/api
            pathType: Prefix
            backend:
              service:
                name: sdkwork-api-memory-standalone-gateway
                port:
                  name: http
          - path: /app/v3/api
            pathType: Prefix
            backend:
              service:
                name: sdkwork-api-memory-standalone-gateway
                port:
                  name: http
          - path: /backend/v3/api
            pathType: Prefix
            backend:
              service:
                name: sdkwork-api-memory-standalone-gateway
                port:
                  name: http
