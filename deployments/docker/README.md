# Deployments

Container, Kubernetes, systemd, nginx, and release handoff examples for SDKWork Memory cloud deployment profile.

Phase 1 ships contract verification and API-server bootstrap skeleton only; full packaging targets are declared in `sdkwork.workflow.json`.
